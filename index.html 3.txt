<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TIO | Marketplace</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Outfit', sans-serif; background-color: #ffffff; color: #111; transition: background-color 0.3s; }
        .dark-mode { background-color: #0a0a0a; color: #f9f9f9; }
        .dark-mode .bg-white { background-color: #1a1a1a; border-color: #333; }
        .page { display: none; }
        .page.active { display: block; }
        #promo-text { transition: opacity 0.5s ease-in-out; }
        .blur-nav { backdrop-filter: blur(8px); background: rgba(255, 255, 255, 0.8); }
        .dark-mode .blur-nav { background: rgba(10, 10, 10, 0.8); }
        .title-accent { position: relative; }
        .title-accent::after { content: ''; position: absolute; left: 0; bottom: -5px; width: 100%; height: 4px; background: currentColor; transform: skewX(-20deg); }
        .star-float { animation: float 3s infinite ease-in-out; display: inline-block; }
        @keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
        /* Perbaikan bug bingkai: memastikan border selalu tampil */
        .item-card { border: 2px solid #e5e7eb; box-sizing: border-box; }
        .dark-mode .item-card { border: 2px solid #333; }
    </style>
</head>
<body class="min-h-screen flex flex-col">

    <nav class="sticky top-0 z-50 flex justify-between items-center py-6 px-6 md:px-20 blur-nav border-b border-transparent transition">
        <h1 class="text-3xl font-black tracking-tighter cursor-pointer title-accent" onclick="showPage('home')">TIO.</h1>
        <button onclick="toggleTheme()" class="text-xs font-bold uppercase tracking-widest border-2 border-black dark:border-white px-6 py-2 rounded-full hover:bg-black hover:text-white dark:hover:bg-white dark:hover:text-black transition">Tema</button>
    </nav>

    <div class="text-center py-4 px-6">
        <p id="promo-text" class="text-sm font-bold uppercase tracking-[0.1em] opacity-60">KUALITAS TERBAIK UNTUK ANDA</p>
    </div>

    <div id="content" class="px-6 md:px-20 pb-20 flex-grow">
        <main id="home" class="page active">
            <header class="py-12 relative">
                <h2 class="text-5xl md:text-8xl font-black mb-4 leading-none relative inline-block">
                    Pilihan Terbaik Anda.
                    <span class="absolute -top-10 -right-12 text-3xl star-float text-yellow-500">✦</span>
                    <span class="absolute top-0 -left-12 text-xl star-float text-yellow-400">✦</span>
                    <span class="absolute -bottom-8 right-20 text-2xl star-float text-orange-400">✦</span>
                    <span class="absolute -top-12 right-20 text-lg star-float text-yellow-300">✦</span>
                </h2>
            </header>
            <section class="grid grid-cols-2 md:grid-cols-3 gap-6 py-6">
                <div onclick="showPage('smartphone')" class="bg-white item-card p-6 rounded-[2rem] hover:border-black dark:hover:border-white transition cursor-pointer text-center relative overflow-hidden group">
                    <div class="text-3xl mb-2">📱</div><h3 class="font-black">Smartphone</h3>
                </div>
                <div onclick="showPage('mobil')" class="bg-white item-card p-6 rounded-[2rem] hover:border-black dark:hover:border-white transition cursor-pointer text-center relative overflow-hidden group">
                    <div class="text-3xl mb-2">🚗</div><h3 class="font-black">Mobil</h3>
                </div>
                <div onclick="showPage('motor')" class="bg-white item-card p-6 rounded-[2rem] hover:border-black dark:hover:border-white transition cursor-pointer text-center relative overflow-hidden group">
                    <div class="text-3xl mb-2">🏍️</div><h3 class="font-black">Motor</h3>
                </div>
                <div onclick="showPage('laundry')" class="bg-white item-card p-6 rounded-[2rem] hover:border-black dark:hover:border-white transition cursor-pointer text-center relative overflow-hidden group">
                    <div class="text-3xl mb-2">🧺</div><h3 class="font-black">Laundry</h3>
                </div>
                <div onclick="showPage('kontrakan')" class="bg-white item-card p-6 rounded-[2rem] hover:border-black dark:hover:border-white transition cursor-pointer text-center relative overflow-hidden group">
                    <div class="text-3xl mb-2">🏠</div><h3 class="font-black">Kontrakan</h3>
                </div>
            </section>
        </main>

        <script>
            function createPage(id, title, products) {
                document.write(`<main id="${id}" class="page">
                    <button onclick="showPage('home')" class="mb-6 text-xs font-bold underline uppercase">« Kembali</button>
                    <h2 class="text-4xl font-black mb-8 relative inline-block">${title} <span class="text-yellow-500">✦</span></h2>
                    <div class="space-y-4">
                        ${products.map(p => `
                        <div class="bg-white item-card p-6 rounded-2xl flex flex-col md:flex-row items-center justify-between gap-4 transition hover:border-black">
                            <div><h4 class="font-bold text-lg">${p.name}</h4><p class="text-sm opacity-60">${p.price}</p></div>
                            <button onclick="redirectToIG()" class="bg-black text-white px-8 py-3 rounded-full text-xs font-black hover:scale-105 transition">${p.action}</button>
                        </div>`).join('')}
                    </div>
                </main>`);
            }

            createPage('smartphone', 'Smartphone', [
                {name: 'iPhone 15 Pro', price: 'Rp 15.000.000', action: 'BELI'},
                {name: 'Samsung S24 Ultra', price: 'Rp 18.000.000', action: 'BELI'}
            ]);
            createPage('mobil', 'Mobil', [{name: 'Tesla Model 3', price: 'Rp 2.000.000/hari', action: 'SEWA'}]);
            createPage('motor', 'Motor', [{name: 'Yamaha NMAX', price: 'Rp 150.000/hari', action: 'SEWA'}]);
            createPage('laundry', 'Laundry', [
                {name: 'Cuci Komplit 5kg', price: 'Rp 25.000', action: 'PESAN'},
                {name: 'Cuci Bed Cover', price: 'Rp 35.000', action: 'PESAN'}
            ]);
            createPage('kontrakan', 'Kontrakan', [{name: 'Kontrakan Strategis', price: 'Rp 1.000.000/bulan', action: 'PESAN'}]);
        </script>
    </div>

    <footer class="py-10 text-center border-t mt-auto">
        <a href="https://www.instagram.com/fuzion_oo" target="_blank" class="inline-block px-8 py-3 border-2 border-black dark:border-white rounded-full text-xs font-bold uppercase tracking-widest hover:bg-black hover:text-white dark:hover:bg-white dark:hover:text-black transition">Follow Instagram</a>
        <p class="mt-6 text-[11px] font-bold uppercase tracking-[0.2em] opacity-50">@TIO GROUP INDONESIA <span class="text-yellow-500">✦</span></p>
    </footer>

    <script>
        const promos = ["KUALITAS TERBAIK UNTUK ANDA", "HARGA TERMURAH SE-INDONESIA", "LAYANAN CEPAT DAN AMAN", "STOK TERBATAS, PESAN SEKARANG"];
        let currentPromo = 0;
        const promoEl = document.getElementById('promo-text');
        setInterval(() => {
            promoEl.style.opacity = 0;
            setTimeout(() => {
                currentPromo = (currentPromo + 1) % promos.length;
                promoEl.innerText = promos[currentPromo];
                promoEl.style.opacity = 1;
            }, 500);
        }, 4500);

        function toggleTheme() { document.body.classList.toggle('dark-mode'); }
        function showPage(pageId) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            const p = document.getElementById(pageId);
            if(p) p.classList.add('active');
            window.scrollTo(0,0);
        }
        function redirectToIG() {
            window.open('https://www.instagram.com/fuzion_oo?igsh=M2JweHE0aTRrYmhm', '_blank');
        }
    </script>
</body>
</html>

